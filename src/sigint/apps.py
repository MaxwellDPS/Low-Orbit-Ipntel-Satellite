from django.apps import AppConfig

# pylint: disable = missing-class-docstring, invalid-name
class sigintConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'sigint'